{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [
    "FIRECRAWL_API_KEY",
    "JINA_API_KEY",
    "OPENAI_API_KEY",
    "DATABASE_URL",
    "EXPO_PUBLIC_API_URL"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**", ".expo/**"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "typecheck": {
      "dependsOn": ["^typecheck"]
    }
  }
}
