import { tv } from "tailwind-variants";

export const relatedSpacesStripVariants = tv({
  slots: {
    section: "pt-4 pb-2",
    header: "mb-2 px-1 text-xs uppercase tracking-wide text-muted",
  },
});
